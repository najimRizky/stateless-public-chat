{
  "name": "backend-websocket",
  "version": "1.0.0",
  "description": "my first web socket",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "node src/index.js",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q dist/api/index.js\"",
    "ts.check": "tsc --project tsconfig.json",
    "add-build": "git add dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pre-commit": [
    "ts.check",
    "build",
    "add-build"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.5.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.11.9",
    "@types/socket.io": "^3.0.2",
    "concurrently": "^7.5.0",
    "nodemon": "^2.0.20",
    "pre-commit": "^1.2.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  }
}